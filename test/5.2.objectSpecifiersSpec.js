describe("object specifiers pattern", function(){
  it("should be used with objects with a large number of parameters for example", function(){
		/*var maker = function(args){
			this.first = args.first;
			this.last = args.last;
		    this.state = args.state;
		    this.city = args.city;
		    return this;
		}
		var myObject = maker({
		    first: 'first',
		    last: 'last',
		    state: 'state',
		    city: 'city'
		});
		expect(myObject.first).to.be('first');*/
  });
});